# 2026-09-09 實作狀態

## 已落地

Godot 4.7.2 標準版與 Windows x64 debug/release 模板已下載、官方 SHA512 校驗及執行。
隔離 Python 3.13.5、Pillow 11.3.0、NumPy 2.3.2 可執行；Krita 5.3.3 便攜版已解壓。
Ghidra 使用專案內設定與快取成功以 SuperH:BE:32:SH-2 匯入 TWN.BIN，未在煙霧測試中執行分析。
Ymir --help 通過；本輪沒有新增遊戲實機驗收。

Krita 與 Tile Molester 的啟動嘗試未取得可可靠檢查的樣本視窗，因此
「開啟指定檔案並檢查畫面」仍未驗收；程式／JAR 存在不等於工具可用性全部通過。
Real-ESRGAN 與 Audacity 延後至對應素材／音訊整合階段；本輪未安裝或假稱使用。

| 關卡 | 狀態 |
|---|---|
| A 工具驗證 | Godot／Python／Ghidra 核心通過；兩個美術檢查工具的樣本畫面驗收未完成 |
| B 資料可行性 | 具名文字、V1N 索引紋理與一個音訊樣本有證據；整體未通過 |
| C 可玩切片 | 未完成；只有 PC VM／存檔元件與資源驗證台 |
| D 高清與便利功能 | 高清 UI 輸出已驗證；原色素材與高清修整尚未開始 |
| E 發行驗證 | 可匯出本地 Windows 資源驗證台；不是遊戲發行驗收 |

## 測試證據

- Python 8 項測試：來源雜湊、邊界、格式拒絕、譯文錯配、V1N 全記錄往返、SNF、AIFF。
- Godot 14 項 PC 事件與存檔契約檢查，合成測試資料，未冒充原作劇情。
- Godot 8 項驗證台整合檢查，使用真實匯入資料測試翻頁、界限、歷史與不可玩標記。
- 1920×1080、2560×1440、3840×2160 精確渲染輸出，UI 截圖可檢查。
- 渲染短測排除前 60 幀，取後續 180 幀的實際 monotonic clock 間隔。
  這是約三秒的資源驗證台取樣，不是長時間遊玩、戰鬥或最低硬體效能保證。

硬體：RTX 4070 Ti SUPER，16,376 MiB 顯存；系統可見實體 RAM 33,883,017,216 bytes（約 32 GB）。
測試工作台短測約 16.67 ms 平均、16.77 ms P95，具體數值由 reports/qa-* 的 render.json 保存。

## 下一步：Stage B 尚缺的實證

1. MAP001 背景資源、圖塊排列與前景遮擋。先追查 TWN 的場景載入與 VDP2 上傳，
   將來源片段映射到既有畫面；SNF 目前僅識別 12 筆容器目錄，不推定它就是地圖。
2. 主角與 NPC 的 V1N 索引、色盤、透明規則、拼合錨點、鏡射與時序。
   以繪圖命令來源位址對接本輪 242 筆 RAM 命中，再查色彩 RAM／色盤載入。
3. 碰撞、移動邊界及事件位置。不得依截圖自行畫牆後聲稱是原作碰撞。
4. 可達的哈比村事件分支與 NPC 觸發。現有 137 段文字不是可執行事件圖。
5. 一場原作一般戰鬥：行動順序、攻擊／魔法／道具、命中／傷害、勝敗、獎勵與返回狀態。
   現有 EXP 等局部逆向結果可作輸入，但沒有完整戰鬥規則驗證。

上述缺口是逆向工作，不需要使用者猜測格式。完成前不開啟可玩標記，也不增加原有
繁中化專案的 Phase F 百分比或實機通過數。

Git 使用獨立本地 main 倉庫，作者 MK；未配置遠端。需要備份或推送時再提供遠端 URL。
